# Stroke Off — environment variables
# Copy to .env.local and fill in. Never commit real values.

# --- Client (safe to expose; VITE_ prefix is bundled into the app) ---
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# --- Server-side only (Supabase Edge Functions; NEVER expose to the client) ---
# Used by the guest claim/results email function in a later phase.
RESEND_API_KEY=
SUPABASE_SERVICE_ROLE_KEY=
